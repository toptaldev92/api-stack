- swagger: "2.0"
  info:
    title: Alchemy Authors Extraction API.
    description: AlchemyAPI is capable of extracting author information from web pages.
    termsOfService: http://www.alchemyapi.com/company/terms.html
    contact:
      name: AlchemyAPI
      url: http://www.alchemyapi.com/company/contact
      email: questions@alchemyapi.com
    version: v1
  host: access.alchemyapi.com
  basePath: /calls/
  schemes:
  - http
  produces:
  - application/json
  consumes:
  - application/json
  paths:
    /url/URLGetAuthors:
      get:
        summary: Get Authors
        description: AlchemyAPI is capable of extracting author information from web
          pages.
        operationId: callsUrlURLGetAuthors
        parameters:
        - in: query
          name: apikey
          type: string
          format: string
        - in: query
          name: url
          type: string
          format: string
        responses:
          200:
            description: Successful response
            schema:
              type: array
              items:
                $ref: '#/definitions/results'
        tags:
        - Author
        security:
        - apikey: []
  securityDefinitions:
    apikey:
      type: apiKey
      name: apikey
      in: query
  definitions:
    results:
      properties:
        status:
          description: This is a default description.
          type: string
        usage:
          description: This is a default description.
          type: string
        url:
          description: This is a default description.
          type: string
        authors:
          description: This is a default description.
          type: string