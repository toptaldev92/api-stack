- swagger: "2.0"
  info:
    title: 500px
    description: '500px API provides programmatic access to 500px functionality and
      content. Version 1 of the API is limited to the essentials of the website''s functionality:
      viewing feature streams, photo information and comments, as well as user profiles.'
    termsOfService: https://github.com/500px/api-documentation/blob/master/basics/terms_of_use.md
    version: v1
  host: api.500px.com
  basePath: /v1/
  schemes:
  - http
  produces:
  - application/json
  consumes:
  - application/json
  paths:
    /blogs/:
      get:
        summary: Returns a listing of five recent stories (maximum 100 per page).
        description: Returns a listing of five recent stories (maximum 100 per page).
        operationId: returns-a-listing-of-five-recent-stories-maximum-100-per-page
        parameters:
        - in: query
          name: feature
          description: 'Story stream to be retrieved. Default fresh. Recognized values:'
          type: string
        - in: query
          name: feature (required)
          description: Story stream to be retrieved. Default fresh. Recognized values:Global
            featuresfresh
          type: string
        - in: query
          name: page
          description: Return a specific page in the story listing. Page numbering is
            1-based.
          type: string
        - in: query
          name: rpp
          description: The number of results to return. Can not be over 100, default
            5.
          type: string
        - in: query
          name: user
          description: Return stories by a specific user, as displayed on [http://500px.com/:username/stories][]
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Creates a new Story.
        description: Creates a new Story.
        operationId: creates-a-new-story
        parameters:
        - in: query
          name: body (required)
          description: Content of the blog post.
          type: string
        - in: query
          name: latitude
          description: Latitude for the blog post.
          type: string
        - in: query
          name: longitude
          description: Longitude for the blog post.
          type: string
        - in: query
          name: photo_ids
          description: Comma separated list of Photo ID values to post with the blog.
          type: string
        - in: query
          name: tags
          description: Comma separated list of tags.
          type: string
        - in: query
          name: title (required)
          description: Title for the blog post.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /blogs/:id:
      delete:
        summary: Deletes the story.
        description: Deletes the story.
        operationId: deletes-the-story
        parameters:
        - in: query
          name: id (required)
          description: The Story ID to delete.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      get:
        summary: Returns detailed information of a single story.
        description: Returns detailed information of a single story.
        operationId: returns-detailed-information-of-a-single-story
        parameters:
        - in: query
          name: id (required) - Return information for the specific story.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      put:
        summary: Updates the Story.
        description: Updates the Story.
        operationId: updates-the-story
        parameters:
        - in: query
          name: body
          description: Content of the blog post.
          type: string
        - in: query
          name: id (required)
          description: The Blog Post ID to update.
          type: string
        - in: query
          name: latitude
          description: Latitude for the blog post.
          type: string
        - in: query
          name: longitude
          description: Longitude for the blog post.
          type: string
        - in: query
          name: photo_ids
          description: Comma separated list of Photo IDs for photos that are in the
            blog post.
          type: string
        - in: query
          name: tags
          description: Comma separated list of tags.
          type: string
        - in: query
          name: title
          description: Title for the blog post.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /blogs/:id/comments:
      get:
        summary: Returns a listing of twenty comments for a specific Story.
        description: Returns a listing of twenty comments for a specific Story.
        operationId: returns-a-listing-of-twenty-comments-for-a-specific-story
        parameters:
        - in: query
          name: id (required)
          description: The Story ID to get comments for.
          type: string
        - in: query
          name: page
          description: Return a specific page in the comment listing. Page numbering
            is 1-based.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Creates a comment for the Story.
        description: Creates a comment for the Story.
        operationId: creates-a-comment-for-the-story
        parameters:
        - in: query
          name: body (required)
          description: Content of the comment.
          type: string
        - in: query
          name: id (required)
          description: The Story ID to create a comment for.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /collections:
      get:
        summary: Returns a listing of all User's collections and sets.
        description: Returns a listing of all Users collections and sets.
        operationId: returns-a-listing-of-all-users-collections-and-sets
        parameters:
        - in: query
          name: photos_per_collection_limit
          description: The number of photos included in each collection. Can not be
            over 200, default 200.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Creates new a collection.
        description: Creates new a collection.
        operationId: creates-new-a-collection
        parameters:
        - in: query
          name: kind
          description: 'Kind of the Collection to be created Recognized values: 1 -
            Portfolio Set (default), 2 - Profile Set.'
          type: string
        - in: query
          name: path (required)
          description: Path where the collection will be accessible at 500px.com/user/sets/:path.
          type: string
        - in: query
          name: photo_ids
          description: Comma separated list of Photo ID values to post with the blog.
          type: string
        - in: query
          name: position
          description: Position of the collection in the list of collections.
          type: string
        - in: query
          name: title (required)
          description: Title for the collection.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /collections/:id:
      delete:
        summary: Deletes collection.
        description: Deletes collection.
        operationId: deletes-collection
        parameters:
        - in: query
          name: Forkn      n      n        69
          type: string
        - in: query
          name: Starn  nn    n      281
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      get:
        summary: Returns a collection.
        description: Returns a collection.
        operationId: returns-a-collection
        parameters:
        - in: query
          name: image_size - Numerical size of the image to link to, 1 being the smallest
            and 4 being the largest. Multiple image sizes may be specified as image_size[]=2&amp;image_size[]=4.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      put:
        summary: Updates collection.
        description: Updates collection.
        operationId: updates-collection
        parameters:
        - in: query
          name: id (required)
          description: Collection ID.
          type: string
        - in: query
          name: kind
          description: 'Change kind of the Collection. Recognized values: 1 - Portfolio
            Set, 2 - Profile Set.'
          type: string
        - in: query
          name: path
          description: Path where the collection will be accessible at 500px.com/user/sets/:path.
          type: string
        - in: query
          name: photo_ids
          description: Comma separated list of Photo ID values that are in this collection.
          type: string
        - in: query
          name: position
          description: Position of the collection in the list of collections.
          type: string
        - in: query
          name: title
          description: Title for the collection.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /comments/:id/comments:
      post:
        summary: Creates a reply to an existing comment. Comments can only be nested
          one level deep, you cannot reply to a reply of a comment. If a comment has
          a non-null parent_id value then it cannot be replied to.
        description: Creates a reply to an existing comment. Comments can only be nested
          one level deep, you cannot reply to a reply of a comment. If a comment has
          a non-null parent_id value then it cannot be replied to.
        operationId: creates-a-reply-to-an-existing-comment-comments-can-only-be-nested-one-level-deep-you-cannot-reply-t
        parameters:
        - in: query
          name: body (required)
          description: Content of the comment.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos:
      get:
        summary: Returns a listing of twenty (up to one hundred) photos for a specified
          photo stream.
        description: Returns a listing of twenty (up to one hundred) photos for a specified
          photo stream.
        operationId: returns-a-listing-of-twenty-up-to-one-hundred-photos-for-a-specified-photo-stream
        parameters:
        - in: query
          name: exclude
          description: 'String name of the category to exclude photos by. Note: Case
            sensitive, separate multiple values with a comma.'
          type: string
        - in: query
          name: feature
          description: 'Photo stream to be retrieved. Default fresh_today. Recognized
            values:'
          type: string
        - in: query
          name: image_size
          description: 'The photo size to be returned. It has to be an integer: 1 to
            4. Also an array is accepted:'
          type: string
        - in: query
          name: include_states
          description: If set to 1, returns state of the photo for the currently logged
            in user and authenticated request.
          type: string
        - in: query
          name: include_store
          description: If set to 1, returns market infomation about the photo.
          type: string
        - in: query
          name: only
          description: 'String name of the category to return photos from. Note: Case
            sensitive, separate multiple values with a comma.'
          type: string
        - in: query
          name: page
          description: Return a specific page in the photo stream. Page numbering is
            1-based.
          type: string
        - in: query
          name: rpp
          description: The number of results to return. Can not be over 100, default
            20.
          type: string
        - in: query
          name: sort
          description: Sort photos in the specified order.
          type: string
        - in: query
          name: sort_direction
          description: Control the order of the sorting.  You can provide a sort_direction
            without providing a sort, in which case the default sort for the requested
            feature will be adjusted.
          type: string
        - in: query
          name: tags
          description: If set to 1, returns an array of tags for the photo.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Create a new photo on behalf of the user, and receive an upload key
          in exchange.
        description: Create a new photo on behalf of the user, and receive an upload
          key in exchange.
        operationId: create-a-new-photo-on-behalf-of-the-user-and-receive-an-upload-key-in-exchange
        parameters:
        - in: query
          name: aperture
          description: Aperture value
          type: string
        - in: query
          name: camera
          description: 'Make and model of the camera. Note: Please make sure it contains
            sensible information (eg., does not contain cameras make or model twice)'
          type: string
        - in: query
          name: category
          description: A numerical ID for the Category of the photo
          type: string
        - in: query
          name: description
          description: Description for the photo
          type: string
        - in: query
          name: focal_length
          description: Focal length in millimetres, a string representing an integer
            value
          type: string
        - in: query
          name: iso
          description: ISO value
          type: string
        - in: query
          name: latitude
          description: Latitude, in decimal format
          type: string
        - in: query
          name: lens
          description: Lens used to make this photo
          type: string
        - in: query
          name: longitude
          description: Longitude, in decimal format
          type: string
        - in: query
          name: name
          description: Title of the photo
          type: string
        - in: query
          name: privacy
          description: 'Whether to hide the photo from the user profile on the website.
            Otherwise, the photo is only available for use in Collections/Portfolio.
            Recognized values: 1, 0.'
          type: string
        - in: query
          name: shutter_speed
          description: Shutter speed in seconds, represented by string containing a
            rational expression if the value is 1sec.
          type: string
        - in: query
          name: tags
          description: Comma-separated list of tags to apply to the photo.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id:
      delete:
        summary: Deletes the photo from the User's library.
        description: Deletes the photo from the Users library.
        operationId: deletes-the-photo-from-the-users-library
        parameters:
        - in: query
          name: id (required)
          description: The Photo ID to delete.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      get:
        summary: Returns detailed information of a single photo.
        description: Returns detailed information of a single photo.
        operationId: returns-detailed-information-of-a-single-photo
        parameters:
        - in: query
          name: comments
          description: Return comments to the photo in the response. Comments are returned
            in order of creation, 20 entries per page.
          type: string
        - in: query
          name: comments_page
          description: Return the specified page from the comments listing. Page numbers
            are 1-based.
          type: string
        - in: query
          name: image_size
          description: Numerical size of the image to link to, 1 being the smallest
            and 4 being the largest.
          type: string
        - in: query
          name: tags
          description: Returns an array of tags for the photo.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      put:
        summary: Allows the client application to update user-editable information on
          a photo.nnnResource URLnnhttps://api.500px.com/v1/photos/:idnnnnAuthenticationnnRequired;
          OAuth. The client application must use the OAuth access token issued for the
          owner of the p
        description: 'Allows the client application to update user-editable information
          on a photo.nnnResource URLnnhttps://api.500px.com/v1/photos/:idnnnnAuthenticationnnRequired;
          OAuth. The client application must use the OAuth access token issued for the
          owner of the photo to access this resouce.nnnParametersnnThe application must
          provide the ID of the photo to update in the URL of the request. The following
          parameters are recognized when included in the query string or POST body:nnnnname:
          Title of the photo, up to 255 characters in length.nndescription: Text description
          of the photo, up to 65535 characters in length.nncategory: Integer number
          of the category of the photo. See category mapping for exact values.nntags:
          Comma-separated list of tags applicable to this photo.nnadd_tags: Comma-separated
          list of tags to add to this photos existing tags.nnremove_tags: Comma-separated
          list of tags to remove from this photos existing tags.nnshutter_speed: Shutter
          speed value for the photo, internally stored as string.nnfocal_length: Focal
          length value for the photo, internally stored as string.nnaperture: Aperture
          value value for the photo, internally stored as string.nniso: Integer ISO
          value for the photo.nncamera: Make and model of the camera used to take this
          photo.nnlens: Information about the lens used to take this photo.nnlatitude:
          Latitude of the location this photo was taken at represented by a decimal
          number.nnlongitude: Longitude of the location this photo was taken at represented
          by a decimal number.nnnsfw: Boolean value indicating that the photo may contain
          not-safe-for-work content or content not suitable for minors.nnlicense_type:
          Integer number of the license type chosen for this photo. See license type
          mapping for exact values.nnprivacy: Integer value indicating that the photo
          should be shown (0) or hidden (1) on the users profile.nncrop: A hash containing
          keys x, x2, y, y2 and representing coordinates within which the thumbnail
          must be cropped. The crop is made using the top left corner as the origin.
          The points must be given relative to image size 4, an image of at most 900px
          on the larger side. The client application may skip this if the user does
          not wish to change photo thumbnail.nnnnImplementation detailsnnA parameter
          missing from the request will not be updated.  A parameter set to an empty
          string or null value will be interpreted as the user wishing to reset the
          value of the field to its default value.nnYou can use the add_tags and remove_tags
          parameters to manupulate a photos tags without having to send the complete
          list of tags in the request.nnnReturn formatnnA JSON object containing key
          photo, where photo is a Photo object in full format.nnnErrorsnnAll known errors
          cause the resource to return HTTP error code header together with a JSON array
          containing at least status and error keys describing the source of error.nnnn401:
          Invalid OAuth request: The request was refused because the OAuth signature
          is incorrect.nn404: Photo with ID not found: The photo ID provided is not
          known to the system.nn404: Photo with ID has been deleted: The photo has been
          deleted and can not be edited.nn404: Photo with ID belongs to a deactivated
          user: The photo belongs to a user that is no longer active and can not be
          edited.nn400: Bad photo category: An unrecognized photo category value has
          been provided.nn400: Bad license type: An unrecognized license type value
          has been provided.'
        operationId: allows-the-client-application-to-update-usereditable-information-on-a-photoresource-urlhttpsapi500px
        parameters:
        - in: query
          name: 'add_tags: Comma-separated list of tags to add to this photo''s existing
            tags.'
          type: string
        - in: query
          name: 'aperture: Aperture value value for the photo, internally stored as
            string.'
          type: string
        - in: query
          name: 'camera: Make and model of the camera used to take this photo.'
          type: string
        - in: query
          name: 'category: Integer number of the category of the photo. See category
            mapping for exact values.'
          type: string
        - in: query
          name: 'crop: A hash containing keys x, x2, y, y2 and representing coordinates
            within which the thumbnail must be cropped. The crop is made using the top
            left corner as the origin. The points must be given relative to image size
            4, an image of at most 900px '
          type: string
        - in: query
          name: 'description: Text description of the photo, up to 65535 characters
            in length.'
          type: string
        - in: query
          name: 'focal_length: Focal length value for the photo, internally stored as
            string.'
          type: string
        - in: query
          name: 'iso: Integer ISO value for the photo.'
          type: string
        - in: query
          name: 'latitude: Latitude of the location this photo was taken at represented
            by a decimal number.'
          type: string
        - in: query
          name: 'lens: Information about the lens used to take this photo.'
          type: string
        - in: query
          name: 'license_type: Integer number of the license type chosen for this photo.
            See license type mapping for exact values.'
          type: string
        - in: query
          name: 'longitude: Longitude of the location this photo was taken at represented
            by a decimal number.'
          type: string
        - in: query
          name: 'name: Title of the photo, up to 255 characters in length.'
          type: string
        - in: query
          name: 'nsfw: Boolean value indicating that the photo may contain not-safe-for-work
            content or content not suitable for minors.'
          type: string
        - in: query
          name: 'privacy: Integer value indicating that the photo should be shown (0)
            or hidden (1) on the user''s profile.'
          type: string
        - in: query
          name: 'remove_tags: Comma-separated list of tags to remove from this photo''s
            existing tags.'
          type: string
        - in: query
          name: 'shutter_speed: Shutter speed value for the photo, internally stored
            as string.'
          type: string
        - in: query
          name: 'tags: Comma-separated list of tags applicable to this photo.'
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id/comments:
      get:
        summary: Returns a listing of twenty comments for the photo.
        description: Returns a listing of twenty comments for the photo.
        operationId: returns-a-listing-of-twenty-comments-for-the-photo
        parameters:
        - in: query
          name: id (required)
          description: The Photo ID to get comments for.
          type: string
        - in: query
          name: nested - Include this parameter to return the comments in nested format.
          type: string
        - in: query
          name: page
          description: Return a specific page in the comment listing. Page numbering
            is 1-based.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Creates a new comment for the photo.
        description: Creates a new comment for the photo.
        operationId: creates-a-new-comment-for-the-photo
        parameters:
        - in: query
          name: body (required)
          description: Content of the comment.
          type: string
        - in: query
          name: id (required)
          description: The Photo ID to post comments for.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id/favorite:
      delete:
        summary: Removes the photo to user's list of favorites.
        description: Removes the photo to users list of favorites.
        operationId: removes-the-photo-to-users-list-of-favorites
        parameters:
        - in: query
          name: id (required)
          description: ID of the photo to delete from list of favorites.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Adds the photo to user's list of favorites.
        description: Adds the photo to users list of favorites.
        operationId: adds-the-photo-to-users-list-of-favorites
        parameters:
        - in: query
          name: id (required)
          description: ID of the photo the favorite is cast upon.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id/favorites:
      get:
        summary: Returns all users that had favorite that photo.
        description: Returns all users that had favorite that photo.
        operationId: returns-all-users-that-had-favorite-that-photo
        parameters:
        - in: query
          name: page
          description: Return a specific page in the photo stream. Page numbering is
            1-based.
          type: string
        - in: query
          name: rpp
          description: The number of results to return. Can not be over 100, default
            20.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id/report:
      post:
        summary: Allows to report a photo.
        description: Allows to report a photo.
        operationId: allows-to-report-a-photo
        parameters:
        - in: query
          name: id (required)
          description: ID of the photo to report.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id/tags:
      delete:
        summary: Removes tags from the photo. Accepts one or multiple coma separated
          tags.
        description: Removes tags from the photo. Accepts one or multiple coma separated
          tags.
        operationId: removes-tags-from-the-photo-accepts-one-or-multiple-coma-separated-tags
        parameters:
        - in: query
          name: id (required)
          description: The Photo ID to remove tags from.
          type: string
        - in: query
          name: tags (required)
          description: Coma separated tags.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Adds tags to the photo. Accepts one or multiple coma separated tags.
        description: Adds tags to the photo. Accepts one or multiple coma separated
          tags.
        operationId: adds-tags-to-the-photo-accepts-one-or-multiple-coma-separated-tags
        parameters:
        - in: query
          name: id (required)
          description: The Photo ID to add tags for.
          type: string
        - in: query
          name: tags (required)
          description: Coma separated tags.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id/vote:
      post:
        summary: Allows the user to vote for a photo.
        description: Allows the user to vote for a photo.
        operationId: allows-the-user-to-vote-for-a-photo
        parameters:
        - in: query
          name: id (required)
          description: ID of the photo the vote is cast upon.
          type: string
        - in: query
          name: vote (required)
          description: 'vote, values: 0 for dislike or 1 for like.'
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/:id/votes:
      get:
        summary: Returns all users that had liked this photo.
        description: Returns all users that had liked this photo.
        operationId: returns-all-users-that-had-liked-this-photo
        parameters:
        - in: query
          name: page
          description: Return a specific page in the photo stream. Page numbering is
            1-based.
          type: string
        - in: query
          name: rpp
          description: The number of results to return. Can not be over 100, default
            20.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/search:
      get:
        summary: Returns a listing of twenty (up to one hundred) photos from search
          results for a specified tag, keyword, or location.
        description: Returns a listing of twenty (up to one hundred) photos from search
          results for a specified tag, keyword, or location.
        operationId: returns-a-listing-of-twenty-up-to-one-hundred-photos-from-search-results-for-a-specified-tag-keyword
        parameters:
        - in: query
          name: comments_count u2014u00a0Sort by the number of comments, most commented
            first.
          type: string
        - in: query
          name: created_at
          description: 'Default: sort by time of upload, most recent first.'
          type: string
        - in: query
          name: exclude
          description: 'String name of the category to exclude from the results. Note:
            Multiple categories may be given, separated by commas, e.g. People,Nude'
          type: string
        - in: query
          name: favorites_count
          description: Sort by the number of favorites, most favorited first.
          type: string
        - in: query
          name: geo
          description: A geo-location point of the format latitude,longitude,radius&lt;units&gt;.
            Acceptable units are km or mi.
          type: string
        - in: query
          name: highest_rating
          description: Sort by highest rating achieved, highest rated first.
          type: string
        - in: query
          name: image_size
          description: 'The photo size to be returned. It has to be an integer: 1 to
            4.'
          type: string
        - in: query
          name: 'license_type -- Restrict the results to one or more license types.  Multiple
            types can be separated with a comma: license_type=1,4.'
          type: string
        - in: query
          name: only
          description: 'String name of the category to return photos from. Note: Multiple
            categories may be given, separated by commas, e.g. Fashion,Black and White'
          type: string
        - in: query
          name: page
          description: Return a specific page. Page numbering is 1-based.
          type: string
        - in: query
          name: rating
          description: Sort by current rating, highest rated first.
          type: string
        - in: query
          name: rpp
          description: The number of results to return. Can not be over 100, default
            20.
          type: string
        - in: query
          name: sort u2014u00a0Sort photos in the specified order. The following values
            are recognized:nnnn_score
          description: Sort by query score, best match first.
          type: string
        - in: query
          name: tag
          description: A complete tag string to search for.
          type: string
        - in: query
          name: tags
          description: Returns an array of tags for each photo.
          type: string
        - in: query
          name: taken_at u2014u00a0Sort by the original date of the image extracted
            from metadata, most recent first (might not be available for all images).
          type: string
        - in: query
          name: term
          description: A keyword to search for.
          type: string
        - in: query
          name: times_viewed u2014u00a0Sort by the number of views, most viewed first.
          type: string
        - in: query
          name: votes_count u2013 Sort by the number of votes, most voted on first.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /photos/upload:
      post:
        summary: This is a new photo upload endpoint. It is currently in beta.nCreate
          a new photo on behalf of the user and upload a file.nfile parameter is passed
          in multipart/form-data, other parameters are passed as query parametrs or
          multipart/form-data.
        description: This is a new photo upload endpoint. It is currently in beta.nCreate
          a new photo on behalf of the user and upload a file.nfile parameter is passed
          in multipart/form-data, other parameters are passed as query parametrs or
          multipart/form-data.
        operationId: this-is-a-new-photo-upload-endpoint-it-is-currently-in-betacreate-a-new-photo-on-behalf-of-the-user-
        parameters:
        - in: query
          name: aperture
          description: Aperture value
          type: string
        - in: query
          name: camera
          description: 'Make and model of the camera. Note: Please make sure it contains
            sensible information (eg., does not contain cameras make or model twice)'
          type: string
        - in: query
          name: category
          description: A numerical ID for the Category of the photo
          type: string
        - in: query
          name: description
          description: Description for the photo
          type: string
        - in: query
          name: file (required)  - Photo filename in JPG/JPEG, passed along with multipart/form-data.
          type: string
        - in: query
          name: focal_length
          description: Focal length in millimetres, a string representing an integer
            value
          type: string
        - in: query
          name: iso
          description: ISO value
          type: string
        - in: query
          name: latitude
          description: Latitude, in decimal format
          type: string
        - in: query
          name: lens
          description: Lens used to make this photo
          type: string
        - in: query
          name: longitude
          description: Longitude, in decimal format
          type: string
        - in: query
          name: name
          description: Title of the photo
          type: string
        - in: query
          name: privacy
          description: 'Whether to hide the photo from the user profile on the website.
            Otherwise, the photo is only available for use in Collections/Portfolio.
            Recognized values: 1, 0.'
          type: string
        - in: query
          name: shutter_speed
          description: Shutter speed in seconds, represented by string containing a
            rational expression if the value is 1sec.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /upload:
      post:
        summary: Allows an application to upload the photo file.nnNote that this endpoint
          is at the upload.500px.com domain, not the api.500px.com domain.
        description: Allows an application to upload the photo file.nnNote that this
          endpoint is at the upload.500px.com domain, not the api.500px.com domain.
        operationId: allows-an-application-to-upload-the-photo-filenote-that-this-endpoint-is-at-the-upload500pxcom-domai
        parameters:
        - in: query
          name: file
          description: The multipart HTTP upload. This is expected to be of the same
            format as when a file is being sent from an HTML file form.
          type: string
        - in: query
          name: photo_id
          description: The ID of the photo the file is being uploaded for. The ID should
            be known to the client from the POST photos request result.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /users:
      get:
        summary: Returns the profile information for the current user.
        description: Returns the profile information for the current user.
        operationId: returns-the-profile-information-for-the-current-user
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /users/:id/followers:
      get:
        summary: Returns a list of users who follow the specified user.
        description: Returns a list of users who follow the specified user.
        operationId: returns-a-list-of-users-who-follow-the-specified-user
        parameters:
        - in: query
          name: id (required)
          description: ID of the user.
          type: string
        - in: query
          name: page
          description: Return the specified page of the resource. Page numbering is
            1-based.
          type: string
        - in: query
          name: rpp
          description: Results Per Page, default 20, max 100.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /users/:id/friends:
      delete:
        summary: Removes the user from the list of followers.
        description: Removes the user from the list of followers.
        operationId: removes-the-user-from-the-list-of-followers
        parameters:
        - in: query
          name: id (required)
          description: ID of the User to remove from the followers list.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      get:
        summary: Returns a list of friends for the specified user.
        description: Returns a list of friends for the specified user.
        operationId: returns-a-list-of-friends-for-the-specified-user
        parameters:
        - in: query
          name: id (required)
          description: Return information for the specified user ID
          type: string
        - in: query
          name: page
          description: Return the specified page of the resource. Page numbering is
            1-based.
          type: string
        - in: query
          name: rpp
          description: Results Per Page, default 20, max 100.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
      post:
        summary: Add the user to the list of followers.
        description: Add the user to the list of followers.
        operationId: add-the-user-to-the-list-of-followers
        parameters:
        - in: query
          name: id (required)
          description: ID of the User to add to the followers list.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /users/search:
      get:
        summary: Return listing of ten (up to one hundred) users from search results
          for a specified search term.
        description: Return listing of ten (up to one hundred) users from search results
          for a specified search term.
        operationId: return-listing-of-ten-up-to-one-hundred-users-from-search-results-for-a-specified-search-term
        parameters:
        - in: query
          name: term (required)
          description: A keyword to search for.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
    /users/show:
      get:
        summary: Returns the profile information for a specified user.
        description: Returns the profile information for a specified user.
        operationId: returns-the-profile-information-for-a-specified-user
        parameters:
        - in: query
          name: email
          description: Return information for the user with the specified email address.
          type: string
        - in: query
          name: id
          description: Return information for the specified user ID.
          type: string
        - in: query
          name: username
          description: Return information for the user with the specified username.
          type: string
        responses:
          200:
            description: OK
        tags:
        - ""
        security: []
  definitions: []