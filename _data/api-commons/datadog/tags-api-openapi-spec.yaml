agger: "2.0"
info:
  title: Tags API
  version: 1.0.0
schemes:
- http
produces:
- application/json
consumes:
- application/json
paths:
  /api/v1/tags/hosts:
    get:
      summary: Get Tags
      description: |2-

                  Return a mapping of tags to hosts for your whole infrastructure
      operationId: get-tags
      responses:
        200:
          description: OK
      tags:
      - Tags
definitions: []