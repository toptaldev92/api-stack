agger: "2.0"
info:
  title: Foursquare
  description: 'The Foursquare API provides methods for accessing a resource such
    as a venue, tip, or user. In spirit with the RESTful model, each resource is associated
    with a URL. '
  termsOfService: https://developer.foursquare.com/overview/community
  version: 1.0.0
host: api.foursquare.com
basePath: /v2
schemes:
- http
produces:
- application/json
consumes:
- application/json
paths: []
definitions: []